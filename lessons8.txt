2. def fib(n):
    if n == 1 or n == 2:
        return 1
    else:
        return fib(n - 1) + fib(n - 2) n = int(input())
print(fib(n))

1.
def qwer():
      a = int(input())
      if a==0:
            print(0)
            return 0
      else:
            return qwer()
            print (a)
